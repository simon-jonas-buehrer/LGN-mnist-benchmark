# python
.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/

# MNIST is downloaded on first run, not committed
data/

# local scratch and cluster job logs
.local/

# Generated Verilog: big, and it regenerates from submission.py + the seed. results.json is the
# record of what was measured, and IS committed -- along with the Pareto plot in results/.
records/*/*/artifacts/
*.pt
*.pkl
